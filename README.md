# Simple Calculator

This repository contains a small command-line calculator written in Python.

## Usage

```
python calculator.py <operation> x y
```

Where `<operation>` is one of:
- `add`
- `subtract`
- `multiply`
- `divide`

Example:

```
python calculator.py add 2 3
```

